# Plant Editor API Keys
# Multiple keys separated by commas (one per machine/user)
# Generate with: openssl rand -hex 32
PLANT_EDITOR_API_KEYS=key1,key2,key3

# PostgreSQL password
# Generate with: openssl rand -base64 32
POSTGRES_PASSWORD=your-secure-password-here
